apiVersion: v1
kind: Service
metadata:
  name: mlops-serving-service
  namespace: ml-training
  labels:
    app: mlops-serving
spec:
  type: ClusterIP
  selector:
    app: mlops-serving
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
